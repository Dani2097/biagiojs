/**
 * Wrapper per biagio.config.js — attiva IntelliSense con `import { defineConfig } from 'biagiojs/config'`.
 * @template {import('../../types/index.d.ts').BiagioConfig} T
 * @param {T} config
 * @returns {T}
 */
export function defineConfig(config) {
  return config;
}

/** @typedef {import('../../types/index.d.ts').BiagioConfig} BiagioConfig */
/** @typedef {import('../../types/index.d.ts').BiagioSiteConfig} BiagioSiteConfig */
